#!/usr/bin/env ruby
# frozen_string_literal: true

<% dir = standalone_dir ? "../.linters" : ".." %>
ENV["BUNDLE_GEMFILE"] ||= File.expand_path("<%= dir %>/Gemfile", __dir__)

require "rubygems"
require "bundler/setup"

load Gem.bin_path("yard", "yard")
