## Деплой

```
bundle exec cap production deploy
```

С помощью аргумента `BRANCH` можно указывать ветку для деплоя, по умолчанию деплоится `master`.

```
BRANCH=develop bundle exec cap production deploy
```
