<% if logged_in? %>
  <p><%= current_user.email %></p>
  <%= form_tag session_path, method: :delete do %>
    <%= submit_tag 'log out' %>
  <% end %>
<% else %>
  <p>
    <%= link_to '/auth', session_path %>
  </p>
<% end %>
